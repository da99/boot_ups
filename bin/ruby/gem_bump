#!/usr/bin/env bash
# -*- bash -*-
#
#!/usr/bin/env node
# -*- js   -*-
#
#!/usr/bin/env lua
#
#
set -u -e -o pipefail

ver=$(cat VERSION)

rm -f ./*.gem
git add .
git add -u
git commit -m "Bump: $ver"
git tag v$ver
git push origin v$ver


